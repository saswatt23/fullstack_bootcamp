var length=parseInt(prompt("enter the length of the array"))
var array=[];

for(i=0;i<length;i++){

     array[i]=parseInt(prompt("enter the number"));
}
console.log(array);


     for(i=0;i<length;i++)
     {
      if((array[i]%2)==0 && (array[i])%3)
      console.log("the numbers which are divided ny 2 and 3 are"+array[i])
      
  
}

